# 隐私说明

## 发送的数据

插件仅在用户点击翻译后，将所选 PDF 页面中未命中本地缓存的正文段落发送至用户当前选择的 AI API 或本地模型服务，用于生成中文译文和一句话摘要。插件会跳过参考文献、页码、重复页眉页脚、表格主体和图像内部 OCR 文本。

## 本地数据

每个 AI 配置的 API Key 使用 Zotero/Firefox 登录存储分开保存。Base URL、模型和配置名称保存在 Zotero Preferences 中，不包含密钥。译文缓存保存于 Zotero 数据目录的 `ai-reader-cache` 子目录。插件不读取或修改 `zotero.sqlite`，不修改原 PDF。

“复制整篇 PDF”只获取当前 Zotero 附件的本地路径，并将该文件写入操作系统剪贴板。当附件使用 UUID 或通用文件名时，插件会在系统临时目录中创建内容相同、使用论文标题命名的副本，并在超过 24 小时后清理；不会重命名或修改 Zotero 原附件。“复制提问”只将按用户模板生成的文本写入剪贴板。这两个操作都不会由插件自动上传数据。只有用户主动将 PDF 或文本粘贴到 ChatGPT 等第三方服务后，该服务才会收到相应内容。

## 不收集的数据

插件不收集遥测数据，不读取 cookie，不启动本地服务，不将 API Key、原文或译文写入日志。审计记录只保存段落 ID、字符数和 SHA-256 指纹等元数据。

## 你的责任

请勿将受保密协议、未公开研究或其他无权向第三方披露的内容发送至云端 API。用户选择的服务商对其服务端数据处理负责；请阅读对应的服务条款和隐私政策。使用自定义 URL 时，请确认该服务由你信任的主体运营。
