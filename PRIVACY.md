# 隐私说明

## 发送的数据

插件仅在用户点击翻译后，将所选 PDF 页面中未命中本地缓存的正文段落发送至 DeepSeek API，用于生成中文译文和一句话摘要。插件会跳过参考文献、页码、重复页眉页脚、表格主体和图像内部 OCR 文本。

## 本地数据

DeepSeek API Key 使用 Zotero/Firefox 登录存储保存。译文缓存保存于 Zotero 数据目录的 `ai-reader-cache` 子目录。插件不读取或修改 `zotero.sqlite`，不修改原 PDF。

## 不收集的数据

插件不收集遥测数据，不读取 cookie，不启动本地服务，不将 API Key、原文或译文写入日志。审计记录只保存段落 ID、字符数和 SHA-256 指纹等元数据。

## 你的责任

请勿将受保密协议、未公开研究或其他无权向第三方披露的内容发送至 API。DeepSeek 对其服务端的数据处理负责；请同时阅读其适用的服务条款和隐私政策。
